services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: w6m-web
    ports:
      - "3008:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
